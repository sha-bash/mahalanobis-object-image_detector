[build-system]
requires = ["setuptools>=65"]
build-backend = "setuptools.build_meta"

[project]
name = "moid"
version = "0.1.0"
description = "Few-shot / zero-shot object detection via VLM captions and Mahalanobis distance (mcd)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.24",
  "pandas>=1.5",
  "scipy>=1.11",
  "sentence-transformers>=2.2",
  "joblib>=1.3",
  "pillow>=10.0",
  "pyyaml>=6.0",
]

[project.optional-dependencies]
gigachat = ["langchain-gigachat>=0.5", "python-dotenv>=1.0"]
ocr = ["easyocr>=1.7"]
dino = ["transformers>=4.40", "torch>=2.1"]
video = ["opencv-python>=4.8"]
visual = ["transformers>=4.40", "torch>=2.1"]
yolo = ["ultralytics>=8.0"]
metrics = ["pycocotools>=2.0.7"]
fusion = ["ensemble-boxes>=1.0.9"]
quality = ["pycocotools>=2.0.7", "ensemble-boxes>=1.0.9", "ultralytics>=8.0"]
dev = ["pytest>=7.0"]

[project.scripts]
moid = "moid.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
